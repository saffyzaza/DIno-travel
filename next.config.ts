/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['z-cdn-media.chatglm.cn', 'googleusercontent.com', 'assets.telegraphindia.com'],
  },
}

module.exports = nextConfig